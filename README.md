# devorajs-docs

A [devora.js](https://github.com/hassanalsa3aka/devora.js) project.

## Apps

- **docs** (`apps/docs`) — auth: `none`, domain: `docs.example.com`

## Getting started

```bash
npm install
npm run dev
```

See `devora.config.ts` to add/remove apps, or run `npx devora add <name>`.
