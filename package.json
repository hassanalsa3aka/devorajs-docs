{
  "name": "devorajs-docs",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "devora dev",
    "build": "devora build",
    "postbuild": "bash scripts/build-docs.sh",
    "start": "devora start"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "dependencies": {
    "@devorajs/core": "^0.2.2",
    "@devorajs/cli": "^0.1.0"
  },
  "devDependencies": {
    "pagefind": "^1.5.2"
  },
  "engines": {
    "node": ">=20"
  }
}
