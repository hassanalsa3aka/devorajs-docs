node_modules/
dist/
# Generated by the postbuild step (see package.json) so it's servable from
# publicDir in both `devora dev` and a real build — regenerated by
# `npm run build`, not source.
assets/pagefind/
.vercel/
.netlify/
netlify/
*.tsbuildinfo

.env
.env.*
!.env.example

.DS_Store
